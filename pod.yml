 apiVersion: v1
 kind: Pod
 metadata:
   name: auth-server
   labels:
     app: web
 spec:
   containers:
     - name: auth-server
       image: py-auth-server-lite:v1
       ports:
         - containerPort: 8088